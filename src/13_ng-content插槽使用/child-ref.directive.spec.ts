import { ChildRefDirective } from './child-ref.directive';

describe('ChildRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildRefDirective();
    expect(directive).toBeTruthy();
  });
});
